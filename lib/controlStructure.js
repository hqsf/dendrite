'use strict';

var key = require('keymaster');
var $ = require('jquery');
var blocks = require('./vendor/sir-trevor');

var controlstructure = function(options) {
    // creates a multiply-linked list or "scene graph"
    // representing the navigable structure of the
    // application.
    console.log('a test');
};

module.exports = controlStructure;
