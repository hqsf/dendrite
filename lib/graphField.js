'use strict';

var key = require('keymaster');
var $ = require('jquery');
var blocks = require('./vendor/sir-trevor');

var graphField = function(options) {
    var element = $('div');

    render = {
    }

    key('n', newArticle);
};

module.exports = graphField;
